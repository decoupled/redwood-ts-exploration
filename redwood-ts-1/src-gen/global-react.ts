// this file doesn't need to be generated dynamically
// these declarations could be provided by @redwoodjs/web, for example
import React_ from "react"
declare global {
  // make React globally available
  const React: typeof React_
}
